## 2026-07-02 — Seam–Horizon-Doppelangriff: Replica-Contract (Beweis-Route 1) + GW-Stack GW250114 (Beweis-Route 2)

**(1) Theorie — `experiments/theory-contracts/seam_horizon_replica.py` (17/17 PASS, CONTRACT HOLDS).**
Der Kernel-Identifikations-Rest von SEAM.THEOREM.01 (nach v150/v151/v152: "der RP-Seam-Kernel IST
das BFK-Calderón-Datum") erstmals NUMERISCH am diskretisierten Collar durchgerechnet, nicht nur
symbolisch am abstrakten Skalar: (a) Replica⇒EH-Form am echten Gitter-Kegeloperator, Steigung
2C(γ) auf Defizit-Kegeln (0.01–0.05% Fehler) UND echten Replica-Blättern n=2 (0.75%), n=3 (1.5%);
(b) EH-Koeffizient cutoff-stabil (0.5% Drift), Intercept (ζ-Skala μ_lat) driftet mit der
Diskretisierung = der v152-Anker, an Daten gezeigt — KEIN kanonisches 3/4 taucht auf (ehrlich
protokolliert); (c) BFK/Schur-Split maschinengenau (1e-16), Calderón-Faktor folgt reinem
Schnittkanten-Gesetz E(1−q), Tip-Term ≈ 0 mit Power-Check — v151s "conically clean" am Kernel
selbst gemessen; (d) die Seam-eigenen Transfer-Massen 6ln(3/2), 6ln3 (spec T = {1,(2/3)⁶,(1/3)⁶},
v302) liegen exakt auf der EH-Linie, und der Perron/Attraktor-Mode (m=0) ist nachweislich
IR-divergent (läuft exakt mit −2C·ln(IR)) — **derselbe Recovery-Gap, der den Attraktor eindeutig
macht, macht den induzierten Newton-Koeffizienten endlich.** Offen [O], typtreu: Kontinuums-Limes
(MMST-Klasse = derselbe Einzel-Rest wie SEAM.EQUIV.01) + der eine Anker. Promotion-Kandidat
(würde SEAM.EHMODEL.03s Kernel-Prämisse auf Modell-Ebene schärfen, SEAM.THEOREM.01 NICHT schließen).

**(2) Empirie — GW-Echo-Kanal auf die 10 lautesten Ringdowns ausgeweitet, inkl. GW250114 (SNR 78.6, O4b).**
Neuer Fetcher `fetch_strain_4096.py` (O4+ hat keine 32s-Files auf GWOSC; croppt 4096s-Segmente),
neue Stage 1b `tfpt-gw stack`: Z = Σρ²_max über 23 Detektor-Streams gegen Off-Source-Hintergrund
(20k Realisierungen). Ergebnis (z-korrigiert, s.u.): **STACKED NULL** — Z_on=149.5 vs.
Hintergrund-Median 93.8, stacked p=0.262, kernel-konsistente Streams 0/23 (q̂~1-Exzesse =
Rest-Ringdown, Free-Ratio-Control verwirft). Stage 2 (Walled-Clock) auf GW250114:
NO_KERNEL_RECOVERY. Scorecard-Zeile GW-Echo: data_limited → **consistent** (der Stage-0-Forecast
"Stack erreicht Schwelle" ist ausgeführt; obere Schranke, kein Tension-Claim).

**(2b) Signatur-Revision (gleicher Tag, Nachfrage "könnte die Signatur anders aussehen /
Birefringenz?"): zwei echte Lücken gefunden + geschlossen.** (i) **Rotverschiebung**: der
GWTC-Katalog liefert Quellsystem-Massen, die beobachtete QNM-Frequenz ist f₀/(1+z) — GW190521
(z=0.56) wurde vorher bei 116 Hz statt ~75 Hz gefiltert (~1.6× daneben). Alle Templates auf
Detektor-Frame `M(1+z)` umgestellt (`strain_data.detector_frame_mass`), alle Stufen neu gelaufen.
(ii) **Signatur-Battery Stage 1c** (`tfpt-gw battery`, Bonferroni ×12): Ratio-Semantik
(Amplitude (2/3)⁶ vs. **Energie-Lesart (2/3)³** — die FRB-Lektion Energie↔Amplitude — vs. Step
2/3) × **μ₄-Bounce-Phasen {0, π/2, π, 3π/2}** × Lags 0.5–350 ms (inkl. Planck-Fenster ~0.23 s)
+ Joint-(2,2,0)+(2,2,1)-Subtraktion + Template-Agnostizismus-Kontrolle. Zur Birefringenz-Frage:
Pfad-Birefringenz (Parität, c₋=8) wirkt auf alle Echos eines Events gleich und **kürzt sich im
Inter-Echo-Ratio**; nicht kürzen tut sich nur eine Paritätsphase **pro Reflexion** — das ist
genau der μ₄-Phasenscan (TFPT-natürlich: π/2 pro Bounce). Ergebnis: **NO_VARIANT_ECHO auf allen
10 Events** (bestes Bonferroni-p 0.0157, einzelner Stream, nicht ratio-konsistent, kein
Zweit-Detektor). Der Null ist robust gegen alternative Signatur-Lesarten. Offen dokumentiert:
Lag-Drift zwischen Echos, Präzession.

**(2c) Punkt-Test Stage 1d (`tfpt-gw point`) — die schärfste TFPT-Hypothese, nie zuvor als
Punkt getestet.** Die Battery scannt den Lag frei; TFPT SAGT ihn aber VORHER: Gravastar/Nariai
C=3/8 ⇒ Tortoise-Round-Trip Δt = 2.288·M_det (0.77 ms GW250114, 2.59 ms GW190521). Punkt-Test =
vorhergesagter Lag (±25%) × Kernel-Ratios {(2/3)⁶, (2/3)³} × μ₄-Phasen (Bonferroni nur ×10),
plus eine morphologie-robuste INKOHÄRENTE Pro-Echo-Statistik (freie Phase pro Echo — deckt die
Barrieren-Tiefpass-Lücke der Battery ab). Ergebnis: **NO_POINT_ECHO auf allen 10 Events**
(bestes p_bonf=0.013, GW150914/L1 — derselbe Stream, den die Battery als template-agnostisches
Breitband-Residuum verwirft). Damit ist der schärfste TFPT-spezifische Echo-Test (Theorie-Lag ×
Kernel-Ratio) sauber null; obere Schranke, kein Tension-Claim.

## 2026-07-02 — Schließungsrouten-Kandidaten (Sandbox): SEAM.EQUIV.01 + ALPHA.QUILLEN.EXACT.01 (route_candidate, kein Claim)

**UPDATE (gleicher Tag): PROMOTED.** Beide Routen sind als `verification/v469_seam_crossedproduct_route.py`
(`SEAM.EQUIV.CROSSEDPRODUCT.01`) und `verification/v470_alpha_inflow_level.py` (`ALPHA.QUILLEN.INFLOW.01`)
in die Suite übernommen (10/10 + 7/7, Suite ALL CHECKS PASSED durch v470), Wolfram-Mirror 365→368,
Lean `seamResidualClosed'`-Parallelroute (lake build OK), Ledger/Paper/Website/Changelog synchron.
Beide Targets bleiben [O] — Promotion = Zertifizierungs-Härtung, keine Statusaufwertung.
Details unten (Original-Sandbox-Notiz).

Zwei neue vollständige ZERTIFIZIERUNGS-Routen für die beiden benannten Residuen, als
Sandbox-Skripte `experiments/tfpt-discovery/closure_route_seam_crossedproduct.py` (11/11 PASS)
und `closure_route_alpha_inflow_level.py` (12/12 PASS). NICHTS promoted, kein
Ledger-/Paper-/Lean-Touch; Verdict-Enum: route_candidate.

**(1) SEAM.EQUIV.01 — Crossed-Product-Route + R1→R1'-Invariantenreduktion.**
Die 128-Spinor-Erweiterung SO(16)₁→(E8)₁ braucht das 2025-Preprint-Leg (AGT 2506.01008 /
AMT 2407.18222) nicht als EINZIGEN Träger: h_spinor(so(16)₁) = 16/16 = 1 ∈ ℤ ⇒
Statistikphase +1 ⇒ der Z₂-Simple-Current-Crossed-Product ist eine LOKALE Index-2-Erweiterung
(Longo–Rehren RMP 7 (1995); Böckenhauer RMP 8 (1996) liefert das Level-1-so(N)-Netz MIT
Sektoren aus even-CAR; Böckenhauer–Evans CMP 197 (1998)); KLM μ = 4/2² = 1 ⇒ holomorph ⇒
(E8)₁ per v463-Selektor (dim V₁ = 248, Turm [1,248,4124,34752] re-verifiziert). Parallel-Check
μ₄-Glue (Index 4): h(J^k) = {1,1,1} alle ganzzahlig (deckt sich mit v125-Isotropie q(k,k)=k²).
Zweitens: das Lean-Axiom `collar_realizes` (Modell-Fiat) ist reduzierbar auf
`collar_invariants` — {quasi-frei [C v155/v160], Gap [E v302], Klasse D, c₋=8 [E via v456]}
+ Phasenklassifikation (Kitaev 2006/2009 — 16-fold way: ν=16 ⇒ bosonische (E8)₁-Kante;
c₋-Invarianz: Kapustin–Spodyneiko PRB 101 (2020), Kim et al. PRL 129 (2022)); FHS-Chern
C(M=1)=1 / C(M=3)=0 numerisch bestätigt, ν=16, c₋=8. EHRLICH: Quasi-Freiheit des abstrakten
Seams bleibt [C]; zitierte Sätze bleiben zitiert (Zertifizierung, kein Re-Proof); die exakte
Beispiel-Stelle SO(16)₁⊂(E8)₁ in BE I–III ist bei Promotion zu verifizieren.

**(2) ALPHA.QUILLEN.EXACT.01 — Inflow-Level + Einbettungsrigidität (Vereinheitlichung).**
(a) Das α³-Level k₀ = 1 ist nicht mehr "Einheitslevel per Minimalität" (v435 [C]), sondern
= |C_bulk| = 1, BERECHNET am selben p+ip-Collar-Modell, das S3 realisiert (v367); Quantisierung
zitiert (TKNN 1982 / Avron–Seiler–Simon 1983; Callan–Harvey 1985 Inflow; APS/Witten RMP 88
für η=CS-Anteil von δ log det; Quillen/Bismut–Freed Determinantenlinien-Krümmung ganzzahlig).
(b) Die Seam-F-Normalisierung (v434-[C]) ist der affine Einbettungsindex k_Y = tr(Y²)/tr(T3²)
= (5/6)/(1/2) = 5/3 (Ginsparg 1987): (3/5)·(41/6) = 41/10 = b₁ exakt, 41/6 = 20/3+1/6 aus dem
Carrier-Content — Level-1-Stromalgebra-Rigidität ⇒ kein freier Dial, konditional NUR auf
SEAM.EQUIV.01 (konsistent mit v382 "face of"). STRUKTURELLER KERN: beide Targets sind zwei
Antworten EINER invertiblen Phase — c₋=8 (gravitativ → Seam-Netz) und C=1 (U(1) → α³-Level);
R1' schließen füttert beide. EHRLICH: das Brücken-Lemma "δ log det_ζ(Seam) = Inflow-Antwort"
bleibt benannter zitierter Schritt; EM1-Lesart bleibt [C]; α⁻¹ = 137.0359992168 bleibt [E]
(Wurzel + Split < 1e-35 re-verifiziert).

Promotion-Pfad (wenn gewünscht): neue vN-Module (LR-Lokalitätszertifikat h_s=1 / k_Y-Rigidität /
k₀=|C|) + Lean: `seam_realisation_theorem` auf das 95–01-Paket umgründen (AMT/AGT als zweiter
unabhängiger Zeuge), `collar_realizes` → `collar_invariants`. Bis dahin: beide Targets bleiben
[O] wie im Ledger.

## 2026-07-02 — Muster-Suche (Sandbox): Dritte-Generation-Leiterbasis + dm²-Ratio=|J| (POST-HOC, kein Claim)

Explorativer Lauf `experiments/tfpt-discovery/pattern_hunt_pmns_dressing.py` (reine Sandbox,
NICHTS promoted, kein Scorecard-/Ledger-/Paper-Touch). Zwei Kandidaten-Muster, beide
parameterfrei im EXISTIERENDEN Vokabular, beide ehrlich als post-hoc geflaggt:

**(1) Dritte-Generation-Leiterbasis-Regel — die drei ~2σ-Spannungen sind EIN Muster.**
Die drei unabhängigen ~2σ-Mixing-Spannungen der Suite (sin²θ13 +2.00σ, sin²θ23 +1.76σ,
V_cb +1.79σ) verschwinden simultan, wenn der Seed in den Dritte-Generations-Kanälen durch
die Leiterbasis λ_C² = φ₀(1−φ₀) statt bare φ₀ eintritt (dieselbe Basis, die die
Yukawa-Massenleiter trägt, tfpt_2), plus derselbe −φ₀/2-Shift für θ23 wie im θ12-Record:
- `sin²θ13 = λ_C² e^{-5/6} = 0.021880` → −0.12σ NuFIT (−0.27σ reactor-only v3; Daya-Bay-only +0.20σ, Δχ²=+4.3 vs Record)
- `sin²θ23 = (1−φ₀)/2 = 0.473414` ⇔ **cos 2θ23 = φ₀** → +0.20σ
- `V_cb = λ_C²/(1+λ_C) = 0.041119` → +0.01σ (PDG 0.0411±0.0013)
- 1-2-Kanäle (θ12, λ_C) bleiben UNgedresst korrekt; V_ub verträgt beides (schwach).
Joint-χ² der 4 Dritte-Gen-Mixings: 10.36 → 1.18 (Δχ²=9.18, NULL neue Parameter).
Gewichtete gemeinsame Rel.-Abweichung der 4 Kanäle: −4.92%±1.62% — ≠0 bei 3.0σ,
konsistent mit −φ₀ (+0.24σ). EHRLICH: (1−φ₀)/(1−c3)/(1−λ_C²) sind bei heutiger Präzision
entartet (<1.4% auseinander); Dressing-Census: 5/16 gleich-einfache Dressings fixen die
beiden PMNS-Spannungen ebenfalls (LEE!); alles post-hoc, Record-Freeze (v84/REG.FREEZE.01)
UNangetastet — v328 nennt θ13 den ehrlichen Druckpunkt, dieses Muster wäre die strukturelle
Auflösung, WENN sie je aus dem Transport (v393-Klasse, Schranke (2/3)⁶=8.8% > φ₀=5.3%)
abgeleitet würde. Kill: sin²θ13 stabil auf der 0.0231-Seite ODER V_cb exklusiv ~0.0395.

**(2) Splitting-Ratio-Kandidat: dm²₂₁/dm²₃₁ = |J_PMNS| (bisher UNvorhergesagt).**
Die gefrorene PMNS-Matrix (Record-Winkel + δ=240°) hat |J| = 0.029653; NuFIT 6.0 NO misst
dm²₂₁/dm²₃₁ = 0.029805±0.000796 → **−0.19σ** (äquivalent m2/m3 = √|J| = 0.17220 vs
0.17264±0.00231). Der bisherige tfpt_2-Tabellenwert m2/m3 ~ πφ₀ = 0.16704 liegt bei
**−2.43σ** — der neue Kandidat ist die einzige getestete Form im 1σ-Band (λ_C(1−λ_C)=0.17403
bei +0.60σ als zweite). Konsequenz (m1≈0): Σm_ν = m3(1+√|J|) = 0.0588 eV (≈ Record 0.0586).
EHRLICH: Grammatik-Dichte im 1σ-Fenster ~1.2e-2 (v100-Census) — als Einzeltreffer NICHT
signifikant; der Wert liegt darin, dass |J| das KANONISCHE abgeleitete CP-Invariant der
schon gefrorenen Matrix ist, nicht eine freie Formel. JUNO (σ(dm²)→0.2–0.5%) entscheidet;
Spannung untereinander: mit gedressten Winkeln (Muster 1) wäre |J|=0.028849 (−1.20σ) —
die beiden Kandidaten ziehen bei JUNO-Präzision gegeneinander (guter Diskriminator).

**Nebenbefunde (nur fürs Protokoll, numerologie-geflaggt):** Ω_c = (2/7)(1−1/4π) → −0.30σ
Planck (⇔ Ω_c/Ω_b = 2/(7φ₀); Koeffizienten-LEE: einziger p/q≤12-Hit; kollidiert konzeptionell
mit dem Axion-Spine-DM-Branch = alternative Lesart, NIE doppelt zählen). Gatto-Check
m_s/m_d = 1/λ_C² = 19.86 vs ~20.0(1.0) → −0.14σ (Folklore, konsistent). α_s(M_Z) ~ 3c₃
bei +1.63σ (schwach, verworfen).

**Status-UPDATE (gleicher Tag): als [O]-Kandidaten PROMOTED.** Der Ableitungsversuch
(`derivation_attempt_ladderbase.py`: Bernoulli-Varianz-Lesart λ_Y² = Var[Bernoulli(φ₀)] exakt,
aber nur Interpretation; Resolvent φ₀/(1+φ₀) zu 0.28% entartet; 1-2×2-3-Komposition 6.6× daneben)
liefert KEINE first-principles-Ableitung → beide Befunde als benannte post-hoc **[O]-Kandidaten**
promoviert, Record (v84) UNverändert: `verification/v467_thirdgen_mixing_pattern.py`
(`FLAV.THIRDGEN.PATTERN.01`) + `verification/v468_dm2_ratio_jarlskog.py` (`FLAV.DM2RATIO.01`),
beide 7/7 grün, Suite ALL CHECKS PASSED; Ledger-Rows, Changelog 2026-07-02, zitiert in tfpt_2
(§ Neutrinos: zwei neue Kandidaten-Absätze, m₂/m₃-Tabellenzeile auf den |J|-Komparator retyped,
Oktant-Prosa ehrlich), introduction (Closure-Ledger), tfpt_4 (Live-Board), tfpt_5 (Hyperplane-Caption);
Website predictions.ts (θ13/θ23/NO-Zeilen-Notizen) + README-Zähler v468/462. Symmetrische Kills
preregistriert (JUNO/Belle II); v467 und v468 diskriminieren sich gegenseitig bei JUNO-Präzision.

---

## 2026-07-01 — vier neue/erweiterte Suchflächen-Experimente in die Scorecard integriert (79 → 83 Zeilen)

Vier standalone `experiments/`-Runner (kein verification/website-Touch, nichts `[E]`/`\veri{}`) in die
zentrale `evidence_scorecard.json` + den `README.md`-Katalog eingehängt; Generator grün, Zähler von 79 → 83
Zeilen (**36 consistent, 5 tension, 13 null, 28 data_limited, 1 parked**):

- **proton-decay/** (`tension`, downstream/branch): `τ_p` im OPTIONALEN gegaugeten Carrier-PS→SO(10)-Zweig B
  (v266/v249; NICHT die Default-Boundary-Lesart A). Goldener Kanal `p→e⁺π⁰`: minimaler 16H `4.2e33 yr`
  von Super-K (>2.4e34) ausgeschlossen; nur der eine E8-erlaubte 45 `+(15,1,1)` rettet den Zweig
  (`1.46e35` 1-Loop, konsistent), aber `7.7e33` 2-Loop < Super-K = latente Spannung. `M_PS` = Scalaron-
  Skala (die TFPT-Koinzidenz). Hyper-K (~1e35, `e⁺π⁰`) entscheidet die Dekade. Teilt
  `independence_group=carrier_ps_so10_uv_branch` mit `sin²θ_W=3/8` (eine Unifikations-Story, kein
  Doppelzählen) — der stärkste nah-terminige Kill-Test der Runde.
- **pulsar-glitch-recovery/ PG.07** (`data_limited`): erste echte PHASENVERBUNDENE Vela-Recovery (2024-
  Riesenglitch, `Δν/ν=2.4e-6`, `τ_d={0.39,2.45,15.1}` d, Reichweite 2.55 Kamm-Perioden > J0537 ~1.9).
  Detektor auf der echten Kadenz injection-validiert (Kaskade p≈0.002), aber `ω=2.583` nicht besonders
  (p≈0.23; Shuffle 0.60; ln-τ-Stack 2016/2019/2021/2024 flach p≈0.53). Reichweite NICHT mehr die Wand —
  limitiert durch das öffentliche glatte Glitch-MODELL (nicht das Residual-`ν(t)`) + die ~2%-Amplitude.
- **crust-cooling-comb/** (`data_limited`): die „zweite, floor-terminierte Datenwelt" — NS-Krustenabkühlung
  `kT_eff(t)`, 8 Kühlepisoden / 6 Transienten / 67 Epochen. Jede Kurve range-blind → Kernel-Stack `n_used=0`;
  Superposed-Epoch-Stack (3.08 Perioden) → `ω=2.583` nicht besonders (p≈0.45), λ-Batterie NULL, 7/8 mit
  nonzero Kern-Floor. Injection: `ε=15%` 100%, aber `ε~2%` nur 2% → dichtelimitiert. Firewall: Oberfläche,
  kein Horizont.
- **comb-meta-limit/** (`data_limited`): erstes quantitatives UL-Design auf `ε=exp(−π²/ln((3/2)⁶))=1.73%`.
  Boundary/horizont-scoped = data_limited (KEIN Horizont-Kanal liefert absolutes `ε`); all-channel
  Universal-DSI 95%-UL `ε<0.120` (HKSJ über A1/A4/A5, ~6.9× über 2%, explizit NICHT TFPT); Injection
  self-consistent (inject 5% → recover 4.8%, UL 5.5%, Coverage 0.92).

Befund: **kein neuer unterscheidender Treffer.** proton-decay ist der schärfste dated Kill-Test (Hyper-K
diese Dekade), die drei Kamm-Erweiterungen (Vela-Realdaten, Krusten-Floor-Welt, Meta-UL) schärfen die
Grenze auf die ~2%-Amplitude ein, ohne sie zu erreichen. Alles standalone unter der Firewall, nichts
hochgestuft. (Hinweis: die „vier echte Spannungen"-Prosa im README §1 ist auf die vier Physik-Stresspunkte
+ die fünfte downstream-Branch-Spannung präzisiert.)

---

## 2026-07-01 — v466: sechster Seed-Kanal aus einem NEUEN Messsektor (Leptonmasse m_e/m_μ)

**Frage:** die empirische Über-Bestimmung von φ₀ (v306 leave-one-out, v465 Cross-Sektor)
nutzt fünf Observable aus DREI Messsektoren (Neutrino-Mischung, CMB, Quark-Mischung). Gibt
es einen SAUBEREN sechsten Kanal aus einem *neuen* Sektor?

**Befund (v466_seed_leptonmass_channel.py, SEED.LEPTONMASS.01):** ja — das
Ladungslepton-Massenverhältnis `m_e/m_μ = (12/7) φ₀²` ist ein sechster, unabhängiger
Auslesekanal desselben Achsen-Seeds φ₀ = 1/(6π) + 48 c₃⁴.
- **[N] neuer Sektor:** Rückrechnung von φ₀ aus dem gemessenen POL-Verhältnis
  `m_e/m_μ = 0.004836` (PDG) → φ₀ = 0.053113, nur **−0.11 %** vom Axiom — ein
  Ladungslepton-MASSEN-Reading (Penning-Fallen-Massen), ein 4. Messsektor.
- **[C] Transport-Vorbehalt (ehrlich):** die Formel ist das QUELL-Verhältnis, das Datum
  das POL-Verhältnis; die Quelle→Pol-Verschiebung ist die seam-gapped Korrekturklasse
  (Schranke (2/3)⁶ = 8.78 %, v393), hebt sich für m_e/m_μ aber weitgehend auf → der
  0.11 %-Rest liegt weit im Band. Als KONSISTENTER neuer Sektor aufgenommen, NICHT als
  schärferer Pin.
- **[N] erweitert:** 6-Kanal-Cluster (v306's 5 + dieser) hat χ²/dof < 1, das
  fehlergewichtete Mittel bleibt < 0.1 % am Axiom; mit dem stützenden Schwerquark
  `m_t/m_b` (−0.23 %) ein 7-Kanal-, FÜNF-Sektor-Agreement.
- **[N] Neg/Power:** Daten↔Formel-Shuffle sprengt χ² um >100×; `m_c/m_s = (34/47)/φ₀`
  ist schema-mehrdeutig (FLAG 11–14), ein SCHWACHER Kanal, bewusst nicht gezählt.
- **Ehrlich:** theoretisch weiterhin EIN Seed (v305 Multiplizität 1) — eine EMPIRISCHE
  Stärkung (neuer Messsektor stimmt zu), KEINE theoretische Unabhängigkeit; φ₀ bleibt
  durch die Axiome fixiert, hebt KEINE Vorhersage hoch und schließt KEIN Gate.

Integriert: run_all, script_registry (Cluster `registry`), Ledger SEED.LEPTONMASS.01,
zitiert in tfpt_5_redteam (Seed-Cross-Validation-Firewall-Winbox neben v305/v306/v465),
Changelog 2026-07-01, README-Zähler → v466 (460 Checks); Python-only (Wolfram-README
geflaggt). `bash build.sh audit` grün.

## 2026-06-30 — recovery-comb-domains A5: Nuclear-Transient/AGN-Scheiben-TDE (J2245+3743), echte ZTF-Daten, sauberer NULL

Neuer **`surface`-Kanal A5** in `recovery-comb-domains` (motiviert aus der J2245-Notiz: „kann man so
nach TFPT-Signaturen suchen?"). Objekt: **J2245+3743** = AGN J224554.84+374326.5, **z=2.554**,
2018 um Faktor >40 aufgehellt, ~10⁵⁴ erg, favorisiert als TDE eines >30 M☉-Sterns **in einer
AGN-Akkretionsscheibe** (Graham+2025, Nat. Astron., arXiv:2511.02178).

**Was gebaut wurde** (alles stdlib+numpy, self-contained, kein verification/website-Touch — reines
`experiments/`-Standalone):
- `ent.py`: Fetcher gegen den **öffentlichen IRSA-ZTF-Lichtkurvendienst** (anonym, kein Login) per
  Position → `data/ent/J2245+3743.csv` (`mjd,mag,magerr,band`, nur `catflags==0` + `magerr≤0.30`;
  **1443 gute g/r/i-Epochen** MJD 58242→60967). Reader: pro Band flux=10^(−0.4 mag), Recovery-Origin
  = hellste Epoche, `bin_ln_t` (Median je `ln(t)`-Bin).
- `channel_a5_nuclear_transient()` + `lambda_battery()` (die quake-Style-TFPT-λ-Batterie auf EINER
  Kurve), in `channels.py`; `fetch-ent` in der CLI; `all_channels()` jetzt 8 Kanäle.

**Ergebnis (echte Daten, ehrlich):**
- **zr-Band: 1007 Post-Peak-Epochen, 3.21 Kamm-Perioden → klärt das >2.8-Gate** (zg range-blind 1.8
  nach Binning, ehrlich ausgeschlossen). J2245 ist also genau die **wide-ln(t)-Einzel-Recovery**,
  nach der die Kamm-Suche hungert — nicht range-limitiert am Kernel.
- **Kernel ω=2.58 NICHT besonders, p≈0.80** → sauberer NULL.
- **TFPT-λ-Batterie** (`λ∈{3/2,φ,2,3,4,5,8,(3/2)⁶,30}`, Bonferroni): **global p≈0.08 → kein
  TFPT-Log-Period besonders → NULL**.
- **Firewall:** AGN-Scheiben-TDE = **Akkretion/Central-Engine**, NICHT Horizont — identische
  Legitimität wie A1/A4; ein Treffer wäre universelle-DSI-Koinzidenz, **nie** TFPT-Bestätigung. Der
  NULL ist **well-powered, nicht data-limited**. (z=2.554-Zeitdilatation = multiplikative
  Zeitachsen-Streckung = additive `ln(t)`-Verschiebung → verschiebt nur die Kamm-PHASE, fügt KEINE
  ln-Reichweite hinzu.)

Konsistent mit allen anderen realen Kanälen (A1/A3/A3b/A4/A5 alle sauber NULL). Kanal ist
multi-source-ready (weitere ENTs in `data/ent/` aktivieren den Cross-Source-Stack). **Kein Claim.**

---

## 2026-06-29 — Recherche-Notiz: der α³-Chern-Level, externe Mathematik (NICHT geschlossen)

Ziel dieser Runde war NICHT, `ALPHA.QUILLEN.EXACT.01` zu schließen, sondern die *externe*
Mathematik des einzigen verbliebenen `[O]` (Residuum 2, der kubische `α³`-Term, das „Maxwell
boundary moment" aus EM1/`v48`) ehrlich zu recherchieren und das Beweisziel zu skizzieren. Kein
fabrizierter Abschluss — das Gate bleibt `[O]`, `α⁻¹` bleibt `[E]`.

**Was die Literatur sagt (die `α²`/`α³`-Aufspaltung ist KEIN Ansatz, sondern ein benannter Rahmen):**

- **Dai–Freed, „η-invariants and determinant lines", J. Math. Phys. 35 (1994) 5155** (Theorem 1.9):
  für eine Familie von Dirac-Operatoren auf einem *ungeraddimensionalen* Rand ist die
  exponentielle η-Invariante `τ = exp(iπ(η+h))` ein **Schnitt der inversen Quillen-
  Determinantenlinie** über dem Parameterraum, mit Einheitsnorm in der Quillen-Metrik; ihre
  kovariante Ableitung bzgl. der **Bismut–Freed-Connection** ist die Index-Dichte
  `∇τ = 2πi ∫_{X/Z} Â(Ω)·ch`. Theorem 3.3: „modulo the integers" ist `ξ` glatt — d.h. die
  η/CS-Größe ist **ganzzahlig quantisiert**.
- **Bismut–Freed, „The analysis of elliptic families I/II", CMP (1986):** Krümmung der Quillen-
  Connection = die Grad-2-Komponente der Familien-Index-Dichte (Heat-Kernel-asymptotik). Das ist
  die `α²`-Seite (Calderón = Determinantenlinien-Krümmung, vgl. `v342`/`v433`/`v434`).
- **Redlich, PRL 52 (1984) 18 / Stora–Zumino-Abstieg:** ein kubischer `Tr F³`-Term im Anomalie-
  Polynom (Grad 6) steigt zu einer **Chern–Simons-Form** `ω₅ ∝ ∫ A∧F²` ab; ihr Level ist durch
  den `U(1)³`-Anomalie-Koeffizienten fixiert und unter Large-Gauge-Transformationen zu `ℤ`
  quantisiert (sonst ist `exp(iS)` nicht eindeutig). Genau die `[C]`-Aussage von `v435`.
- **Vassilevich (hep-th/0306138) / Wodzicki:** im *ungeraddimensionalen* Fall übernimmt die
  η-Funktion die Rolle der Randkorrektur; „krümmungsabhängige Terme werden erst ab D≥6 operativ".
  Bestätigt `v435`s π⁰/Metrik-Unabhängigkeit: der `α³`-Level lebt im η-Sektor, nicht im
  Krümmungs-Integral.

**Saubere Identifikation (jetzt mit Sätzen statt Behauptung):**

| Closure-Term | mathematisches Objekt | Status |
|---|---|---|
| `−2c3³·α²` (Calderón) | Krümmung der Quillen-Determinantenlinie (Bismut–Freed), Gilkey `a₄` | `[E]` Struktur, `[C]` exakter Wert (Seam-F-Norm) |
| `α³` (Maxwell) | η-Invariante / CS-Level (Dai–Freed-Schnitt), kubische `U(1)³`-Anomalie, ∈ ℤ | `[C]` (Quantisierung), Ursprung `[O]` |
| Transport | Heat-Kernel-Counterterm `8b1 c3⁶` (`v434`: `b1 = a₄`) | `[E]`/`[C]` |

**Worauf das EM1-Beweisziel WIRKLICH reduziert (der ehrliche Sketch):**

Das EM1-Lemma „`∂_τ log Z_Maxwell = a³` IST das Seam-`U(1)`-CS/η-Level" ist eine **Instanz des
Dai–Freed-Satzes**. Um es from-first-principles zu schließen, fehlt genau:
1. die **Seam-Dirac-Familie** `D_{∂X/Z}` über den `μ4`-Seam-Moduli `Z` explizit konstruieren
   (der ungerade Rand `∂X` = das Seam-3-Mannigfaltigkeit) — das hängt an der Kontinuums-Existenz,
   d.h. an **`SEAM.EQUIV.01`** (das andere `[O]`, MMST `v336`);
2. ihr η/CS-Level = der `U(1)³`-Anomalie-Koeffizient des **Seam-Ladungsinhalts** = der gesuchte
   Unit-Level `1` (analog `v14`: `Tr Y = Tr Y³ = 0` ist der SM-Hyperladungs-Kubik-Check; der
   Seam-`U(1)` ist NICHT die volle SM-Hyperladung, sondern der minimale Rand-`U(1)`).

**Befund:** `ALPHA.QUILLEN.EXACT.01` ist damit bestätigt **eine Facette von `SEAM.EQUIV.01`**
(steht so schon im Ledger): der `α³`-Level ist *berechenbar, sobald* die Seam-Dirac-Familie
existiert. Es sind also nicht zwei unabhängige externe Probleme, sondern eins (Kontinuums-Existenz)
mit einer berechenbaren Folge (das CS-Level). Das ist Typ-A-Mathematik (`v384`) — NICHT von uns
fabrizierbar, aber jetzt mit präzisem Pfad und benannten Sätzen.

**Nicht geschlossen. Kein neues Modul, keine Statusänderung.** `v435` bleibt der schärfste
verifizierbare Stand (π⁰-Test + bedingte Quantisierung); diese Notiz liefert nur die Literatur-
Erdung dafür (Dai–Freed/Bismut–Freed/Redlich) und den konkreten nächsten Beweisschritt, falls die
Seam-Kontinuumskonstruktion (`SEAM.EQUIV.01`) je angegriffen wird.

---

## 2026-06-16 — Strukturfund-Runde (v219–v230, alle integriert)

Elf neue Verifikationsmodule, alle in `run_all.py` (ALL CHECKS PASSED), Wolfram-Mirror 258/258,
Ledger/Changelog/Papers/Website synchron, `AUDIT OK`. Keine neuen freien Parameter, kein Gate verschoben
— es geht um *tiefere Erklärung und Vereinheitlichung* bestehender Zahlen:

- **Warum {2,3,5}**: `v219` (McKay) baut die binäre Ikosaedergruppe `2I` (Ordnung 120) aus Quaternionen,
  ihre 9 Irrep-Grade = affine-E8-Kac-Marken (Summe 30 = h(E8), Quadratsumme 120 = |R⁺(E8)|). E8 ist die
  exzeptionelle Spitze der McKay-Leiter ⇒ {2,3,5} = Ikosaeder-Achsen. Rückwärtszertifikat, kein P2-Beweis.
  `v223` spiegelt die `μ4`-Clock im E8-Coxeter-Zyklus ((ℤ/30)ˣ, Element 7 hat Ordnung 4).
- **CM-Norm-Dualität** `v222`: `41 = N_ℤ[i](5+4i) = 10b1` (EM-Index = Gauß-Norm des Carrier-Glue-Vektors),
  `7 = N_ℤ[ω](3+2ω)` (Scalaron = Eisenstein-Norm) — eine Aussage statt zwei Zufallszahlen. `v230`: das
  Diamond-Center `(7,11,13)` = (Hex-Norm, QBL-Fock-Count, Quadrat-Norm).
- **CP (Target D)** geometrisch lokalisiert: `v220` (Hexagon-Modul `j=0`, `δ=π/3+3λ²` Führungsterm = arg ρ),
  `v225` (Dual-Normal-Frame, CP als Orientierung `Im det(1,d,ρn)=21·sin(π/3)`), `v227` (`248=120+128`
  Magnitude/Phase-Kanal — ersetzt das zu starke „S⁻ = DM"). Deck bleibt ℤ/4, CP ist die ℤ/6-Faser.
- **QEC** `v221`: Seam = finiter Recovery-Code, `‖Tⁿδ‖ ≤ (2/3)^{6n}‖δ‖` (Knill-Laflamme-artig).
- **Konsolidierungen**: `v228` (P2 als Riemann-Roch-Modenzahl eines Grad-4-Divisors), `v229` (Leptonen =
  étale Frobenius-Algebra über `μ6`), `v224` (`F_transfer` = eine Geodäte auf dem Sheet Diamond).

Offene Gates unverändert (P2-Interface, `G_net`/Seam-Realisierung, `F_transfer`, die eine Einheit `v_geo`).
Nächste sinnvolle Schritte bleiben Frontier: Gravastar-`3/8`-Test, E8-Plumbing/Poincaré als Origin-Bild,
`S⁻`/Ramond-Dunkelsektor — jeweils erst mit physikalischem Modell, nicht in die Spine.

---

## Kurzurteil

Der neue Stand ist deutlich reifer. Ihr seid von einer Signatursuche zu einer **typisierten Entscheidungsmaschine** gekommen: 48 Scorecard Zeilen, davon 22 konsistent, 7 null, 14 datenlimitiert, 4 echte Spannungen und 1 geparkter Analograum. Wichtig: Die README sagt jetzt sauber, dass es **keine unterscheidende Entdeckung** gibt. Das ist nicht defensiv, das ist wissenschaftlich gesund. Theorien, die nie schwitzen, sind meistens tot oder Marketing.

Die stärksten aktuellen Punkte sind:

**Konsistenzcluster:** `θ12`, `K⁺→π⁺νν`, CMB `β`, `Ω_b`, Seed Linie, Λ Hierarchie.
**Echte Stresspunkte:** `θ13`, `A_s` bei fixem `N⋆=51.4`, Myon g minus 2 unter Lattice HVP, Axion Hilltop.
**Saubere Nulls:** fast die gesamte FRB Suite.
**Wertvollste offene Tests:** EHT Residual Imaging, Inflation Branch Resolver, Seed Stress v2, DESI `w`, Axion Spine, Leptogenese, Higgs Kritikalität.

Die Scorecard Firewall und die neuen Metadaten sind genau richtig: `independence_group`, `evidence_class`, `discriminative_power`, `decision_horizon` und `hint_flag` verhindern, dass korrelierte Seed Beine wie unabhängige Treffer aussehen. Das README sagt explizit, dass `phi0_seed` und interne Checks nicht als unabhängige externe Treffer zählen. Sehr gut. 

## 1. Was jetzt klar besser ist

### 1.1 Die alte Zähler Inkonsistenz ist praktisch gelöst

Der aktuelle Stand ist jetzt konsistent generiert: 48 Zeilen, 22 consistent, 4 tension, 7 null, 14 data limited, 1 parked. Das steht im README Generator Block und in der aktuellen `evidence_scorecard`. 

Das ist ein echter Qualitätsgewinn. Manuelle Zähler sind wie Quantenfluktuationen in Excel: irgendwann materialisieren sie sich als Reviewer Problem.

### 1.2 FRB ist jetzt methodisch sauber und empirisch eher negativ

Die FRB Suite ist inzwischen stark gegen Numerologie gehärtet: eingefrorener Kernel, Energie versus Amplituden Semantik, Placebos, Surrogate, Free Quotient Null, Look Elsewhere Korrektur. Das Ergebnis ist größtenteils null: FRB.02, FRB.02b, FRB.04, FRB.06, FRB.07, FRB.08 und FRB.09 liefern keinen belastbaren TFPT Kanal. FRB.03 bleibt nur `data_limited` mit `hint_flag`. 

Das ist gut. Nicht weil FRBs die Theorie stützen, sondern weil sie die Theorie disziplinieren. Die Theorie sieht dadurch weniger nach “wir finden überall Muster” aus.

### 1.3 EHT ist zum echten Datensatz geworden

Der EHT Block ist der größte Fortschritt seit dem letzten Check. Es wurden echte M87 2017 Polarimetrie uvfits aus EHT 2023 D01 01 ingestiert, 4 Tage mal 2 Bänder, und aus den Zirkularbasis Visibilities wurden Netto EVPA und Polarisationsfraktion berechnet. Die Band zu Band EVPA Rotation liegt im Mittel bei etwa `+0.9°`, mit impliziertem `RM ≈ 5×10⁵ rad/m²`. 

Aber: Das ist **noch nicht** der TFPT Test. Das ist nur der reale Daten Ingest plus rohe Achromatizitätsdiagnostik. Der eigentliche Test bleibt:

`χ₀res = χ₀obs − χ₀GRMHD`

Danach müssen drei Nulls gleichzeitig bestehen:

`kein λ² Tail`
`1/r² Profil`
`Sign Flip bei effektiver E·B Umkehr`

Die README formuliert das korrekt als `data_limited`, bis GRMHD Imaging und Residual Subtraktion laufen. 

### 1.4 Seed Stress ist jetzt ein echter Stresstest

Der neue Seed Consistency Block ist gut: `β`, `Ω_b`, Cabibbo und `θ13` werden zu `φ0` invertiert, gemeinsam gefittet und per Leave One Out bewertet. Das Ergebnis: der Block hält, aber `θ13` ist der dominante Zug, mit etwa 88 Prozent des χ² und rund `−1.8σ` im Joint Fit. 

Das ist exakt die richtige Diagnose. Nicht “Seed bestätigt”, sondern:

> Der Seed ist kohärent, aber θ13 ist der erste ernsthafte Risskandidat.

## 2. P0 Korrekturen vor dem nächsten Release

### 2.1 Axion Spine ist semantisch doppelt in der Scorecard

Aktuell gibt es zwei Zeilen für denselben Branch:

`axion relic DM.AXION.SPINE.01`
und
`axion relic DM.AXION.SPINE.01 (theta_i=3pi/5)`

Beide sagen im Kern: `θi = 3π/5`, Finite T Solve pending, Akzeptanzband `0.08 bis 0.16`. Eine davon muss weg oder die erste wird zur reinen Definition ohne Scorecard Gewicht. Sonst zählt ihr denselben offenen Test zweimal. Die aktuelle Scorecard zeigt die spätere, präzisere Zeile mit dem eingefrorenen Ω Band. 

**Empfehlung:** Behalte nur die präzisere Zeile:

```json
"observable": "axion relic DM.AXION.SPINE.01 (theta_i=3pi/5)"
```

Dann gehen die Zähler real auf 47 Zeilen und `data_limited` auf 13, sofern sonst nichts geändert wird.

### 2.2 Seed χ² ist widersprüchlich: 1.08 versus 1.23

Im Inhaltsblock steht für die Shared Seed Erweiterung `χ²/dof=1.08`, im Seed Stresstest Abschnitt steht `χ²/dof=1.23`. Aus den angegebenen Joint Pulls ergibt sich plausibel `χ²/dof≈1.23`. Die Scorecard hat aktuell ebenfalls `1.08`.

**Empfehlung:** Eine Quelle der Wahrheit:

```json
"data_value": "4 observables imply same phi0, chi2/dof=1.23"
```

Und ergänzen:

```json
"max_leg_pull_sigma": 2.0,
"dominant_leg": "theta13",
"dominant_chi2_fraction": 0.88
```

`pull_sigma = 2.0` ist für eine Composite Row missverständlich. Es ist kein normaler Einzel Pull, sondern der stärkste Leg Pull.

### 2.3 Shared Seed Kill Condition ist zu hart formuliert

Die Scorecard sagt sinngemäß: “any leg pulls implied phi0 away at >2 sigma”. Gleichzeitig ist `θ13` bei etwa 2σ und der Status ist `consistent`. Das ist logisch knapp an der Stolperkante.

Die README hat die bessere Regel: `θ13 >3σ` löst `transfer-corrected` aus; zwei Beine >3σ killen den Shared Seed Block. Das sollte die Scorecard übernehmen.

```json
"kill_condition": "two independent seed legs >3 sigma, or one leg >5 sigma; theta13 alone >3 sigma flags PMNS theta13 as transfer-corrected"
```

### 2.4 EHT Signaturkatalog ist an einer Stelle stale

Im Signaturkatalog steht bei EHT noch “datenlimitiert (synthetisch)”, aber weiter unten ist der reale EHT Ingest erledigt. 

**Fix:**

```text
EHT: real data ingest + achromaticity diagnostic done; GRMHD residual nulls open
```

Klein, aber wichtig. Sonst sieht es aus, als hätte die README sich selbst überholt. Kommt vor. Auch Theorien brauchen Kaffee.

### 2.5 EHT sollte nicht `phi0_seed` sein

Die EHT Zeile ist aktuell `independence_group = phi0_seed`. Das ist aus meiner Sicht falsch oder zumindest unglücklich. Der EHT Kanal liest `16c3^4 = 1/(256π⁴)` beziehungsweise `δ_top/3`, also einen Top Form oder Horizon Collar Koeffizienten, nicht denselben `φ0` Seed wie β, Ωb, Cabibbo und θ13.

**Empfehlung:**

```json
"independence_group": "c3_topform_horizon"
```

oder, falls die Enums klein bleiben sollen:

```json
"independence_group": "independent"
```

Besser wäre eine neue Gruppe `c3_topform_horizon`, damit Korrelationen ehrlich bleiben.

### 2.6 Λ Hierarchie nicht als `0.00σ` schreiben

Die README nennt Λ Hierarchie `0.00σ`. Das ist rhetorisch stark, aber statistisch unsauber. Es ist eine Abweichung in Größenordnungen, etwa `0.004 dex/order`, keine echte Mess Pull Größe. Die README selbst warnt, dass Λ drei verschiedene Objekte mischen kann und nicht als absolute Λ Vorhersage verkauft werden darf.

**Fix:**

```text
Λ-Hierarchie: dev = 0.004 orders, not pull_sigma
```

Und in der Scorecard:

```json
"pull_sigma": null,
"log_order_deviation": 0.004
```

### 2.7 `stage` braucht einen internen Typ

Der Recovery Channel ist `evidence_class=internal_consistency`, aber `stage=search_target`. Das ist semantisch schief. Kein Drama, aber unschön.

Entweder ergänzt ihr:

```json
"stage": "internal_consistency"
```

oder ihr lasst `stage` nur für empirische Kanäle gelten und macht bei internen Checks:

```json
"stage": "not_applicable"
```

Die aktuelle Trennung über `evidence_class` rettet es, aber sauberer wäre ein eigener Stage Enum.

## 3. Bewertung der Ergebnisse nach Domäne

### 3.1 FRB: Ergebnis negativ, methodisch stark

Bewertung: **B+ für Methodik, D für positive Evidenz, A für Hygiene.**

FRB liefert aktuell keine unterscheidende TFPT Signatur. Das sollte klar gesagt werden. Die Nulls sind aber nützlich, weil sie den Recovery Kernel in realen, schmutzigen Daten gegen Placebos und freie Quotienten getestet haben. Besonders FRB.02b ist wertvoll, weil es verhindert, dass ein beliebiger Quotient nachträglich als TFPT Kernel gelesen wird. 

**Weitere FRB Untersuchungen nur noch, wenn neue Daten kommen.** Kein FRB.10 aus Langeweile. Das Universum ist groß, aber unsere Look Elsewhere Rechnung leider auch.

### 3.2 CMB Seed: stärkster Kohärenzblock, aber korreliert

Bewertung: **B für Kohärenz, C für unabhängige Evidenz.**

ACT DR6 misst `β = 0.215° ± 0.074°`; das liegt nahe bei `0.2424°`, aber die ACT Analyse selbst warnt vor noch nicht vollständig verstandenen Systematiken. ([arXiv][1])

`Ω_b` passt extrem gut, aber Ωb ist ein Standardkosmologie Wert, also schwach unterscheidend. Die Seed Linie ist schön, aber β und Ωb sind keine völlig unabhängigen Planeten. Der neue Seed Stress mit θ13 und Cabibbo ist deshalb wertvoller als die reine β plus Ωb Darstellung.

**Wichtigster nächster Test:** θ13. Wenn θ13 bei höherer Präzision stabil bei circa `0.02195` bleibt und die TFPT Formel `0.0231` nicht nachzieht, muss dieser Leg entweder als Transfer korrigiert markiert werden oder der Seed Block bekommt eine echte Delle.

### 3.3 Neutrinos: θ12 ist stark, θ13 ist der Alarmmelder

Bewertung: **A für θ12, C minus für θ13, B für Gesamtblock.**

`sin²θ12 = 0.306747` ist aktuell sehr stark. JUNO meldet aus den ersten 59.1 Tagen `sin²θ12 = 0.3092 ± 0.0087`, also komfortabel in der Nähe. ([Indico Global (Indico)][2])

`sin²θ13 = 0.0231` liegt dagegen bei etwa `+2σ`. Das ist kein Kill, aber es ist der derzeit sauberste interne Stresstest, weil θ13 am selben Seed hängt wie β, Ωb und Cabibbo.

**Zusatzuntersuchung:** Reactor only Fit getrennt von NuFIT Global. Nutzt Daya Bay, RENO, Double Chooz, JUNO sobald verfügbar. Keine globalen PMNS Priors in denselben Topf, wenn ihr den Seed isoliert stressen wollt.

### 3.4 Inflation: A_s entscheidet die Typisierung

Bewertung: **B für r Vorhersage, C für n_s, klare Spannung bei fixem N⋆.**

Bei fixem `N⋆=51.4` ist `A_s` massiv daneben, `−11.3σ`. Sobald `N⋆` profiliert wird, landet A_s bei `N⋆≈56.1` und alles sieht sauberer aus. Das ist kein kleiner technischer Punkt, sondern die zentrale Typfrage:

> Ist `N⋆=51.4` Prediction of Record, oder ist das Band `[50,60]` Prediction of Record?

Die README empfiehlt bereits: Band ist Record, 51.4 ist Preferred Branch, A_s ist Bridge. Das ist vernünftig. Aber dann muss `A_s fixed` als Branch Stress geführt werden, nicht als gleichwertige Prediction.

CMB S4 ist hier besonders wichtig, weil `r≈0.0045` bei einer Zielpräzision um `σ(r)≈5×10⁻⁴` scharf testbar wird. 

### 3.5 EHT: bester offener Shape Test

Bewertung: **A für Testqualität, C für aktuellen Datenstand.**

EHT ist der beste noch offene astrophysikalische Test, weil TFPT dort nicht nur eine Zahl sucht, sondern eine Form: achromatisch, `1/r²`, Sign Flip. Die reale Datenpipeline ist gestartet, aber der jetzige EVPA Bandvergleich ist noch kein Residual Test. 

**Wichtig:** Zwei nahe Frequenzbänder um 227 bis 229 GHz reichen kaum, um Achromatizität robust von Faraday und Kalibration zu trennen. Für den echten Test braucht ihr Bildraum, nicht nur Netto EVPA, und idealerweise multi epoch plus externe Faraday Kontrolle.

### 3.6 GW Echo: sinnvoll, aber noch Stage 0

Bewertung: **B für Priorisierung, noch keine Evidenz.**

GWTC 5.0 hat laut LVK 390 bestätigte Events insgesamt und 161 neue O4b Events, damit passt eure Katalogzahl. ([LIGO][3])

Der Scorecard Eintrag ist korrekt als `catalog_feasibility` typisiert. Der SNR Reach `ρ_echo≈6.3` heißt: Strain Test lohnt sich. Es heißt nicht: Echo getestet. Die README formuliert das inzwischen sauber. 

### 3.7 Rare Kaon: derzeit schönster downstream Treffer

Bewertung: **A minus als Konsistenz, B minus als unabhängige Evidenz, weil downstream.**

NA62 2016 bis 2024 misst `BR(K⁺→π⁺νν) = (9.6^{+1.9}_{−1.8})×10⁻¹¹`, sehr nah an TFPT `9.45×10⁻¹¹`. ([arXiv][4])

Das ist stark. Aber es ist kein primitiver Compiler Treffer, sondern eine downstream Flavor Bridge mit Short Distance Funktionen. Genau so steht es im README und so sollte es bleiben. 

Der nächste wirklich gute Test ist nicht nochmal K⁺, sondern:

`R_K = BR(KL→π0νν) / BR(K+→π+νν) = 0.35238`

KOTO ist mit `KL` noch weit über dem SM Bereich, aber das kommt. KOTO 2024/2025 setzt aktuell ein Limit um `2.2×10⁻⁹`, also noch datenlimitiert. ([APS Link][5])

### 3.8 Myon g minus 2: kein Verdict, sondern HVP Thermometer

Bewertung: **ambivalent.**

Mit WP2020 dispersiv sieht TFPT gut aus. Mit WP2025 Lattice ist TFPT in Spannung. Fermilab hat 2025 das finale Ergebnis mit 127 ppb Präzision publiziert, aber die entscheidende Unsicherheit ist jetzt die Standard Model HVP Seite. ([arXiv][6])

Interpretation:

> Wenn Lattice HVP konsolidiert und das Residuum nahe Null bleibt, fällt der g minus 2 Seam Vertex Branch. Nicht der Kern.

Das ist genau die richtige Typisierung.

### 3.9 Axion: Hilltop angeschossen, Spine muss jetzt entscheiden

Bewertung: **Hilltop D, Spine offen.**

Der Hilltop Branch ist in echter Spannung, weil die Finite T Rechnung `Ωa h²≈0.66` liefert, also grob Faktor `5.5` zu viel. Das ist bereits in den Dokumenten sauber festgehalten. 

Der Spine Branch `θi=3π/5` ist jetzt der bessere Kandidat, aber noch nicht gerechnet. Durch die semantische Doppelzeile in der Scorecard wird er momentan sogar zu groß gezählt. Erst zusammenführen, dann rechnen.

### 3.10 Dark Energy w: unterschätzt als Kill Watchdog

Bewertung: **A als Watchdog, aktuell data limited.**

TFPT sagt `w = −1`. DESI DR2 Kombinationen liefern je nach Supernova Kombination Hinweise auf dynamische Dunkle Energie im Bereich etwa `2.8σ bis 4.2σ`, aber die Kombinationen sind systematisch und statistisch heikel. ([arXiv][7])

Dieser Kanal ist wichtiger als er im Scorecard Gewicht wirkt. Wenn sich `w≠−1` systematisch kontrolliert festzieht, trifft das direkt die Λ/H₀ Engine. Nicht sofort die gesamte Theorie, aber den Kosmologie Branch ziemlich unangenehm.

## 4. Lohnenswerte Zusatzuntersuchungen

### P0: Scorecard Hygiene

1. Axion Spine Doppelzeile zusammenführen.
2. Seed χ² auf `1.23` normalisieren oder klar erklären, warum zwei Werte existieren.
3. Shared Seed Kill Condition auf die README Regel ändern.
4. EHT Signaturkatalog von “synthetisch” auf “real ingest done, residual open” ändern.
5. EHT `independence_group` aus `phi0_seed` herausnehmen.
6. Λ Pull als `log_order_deviation` statt `σ` führen.
7. Für Composite Rows eigene Felder einführen: `chi2_dof`, `max_leg_pull_sigma`, `dominant_leg`.

Das ist kein kosmetischer Kleinkram. Das ist die Firewall gegen Scheinpräzision.

### P1: EHT Residual Imaging

Das ist der lohnendste echte Astrophysik Test.

Pipeline:

```text
1. EHT 2017 M87 uvfits zu polarimetrischen Bildern rekonstruieren.
2. Mehrere Imaging Pipelines nutzen: eht-imaging, SMILI, DIFMAP Kontrolllauf.
3. GRMHD Bibliothek mit ipole oder vergleichbarem Radiative Transfer erzeugen.
4. χobs in Bildraum auf Photonenring Koordinaten bringen.
5. χGRMHD abziehen.
6. χres auf drei Nulls testen:
   kein λ² Tail
   1/r² Profil
   Sign Flip bei effektiver E·B Umkehr
7. Injection Tests:
   künstliches 1/r² Signal
   Faraday λ² Signal
   D Term Leckage Signal
   EVPA Kalibrationsoffset
```

Wichtig: Mit nur zwei nahen Frequenzbändern darf die Frequency Null nicht zu stark behauptet werden. Macht sie als Diagnose, nicht als finalen Achromatizitätsbeweis.

### P1: Seed Consistency v2

Nächste Version:

```text
phi0_from_beta
phi0_from_Omega_b
phi0_from_Cabibbo
phi0_from_theta13_reactor_only
phi0_from_theta13_global
```

Dazu:

```text
correlation matrix
leave one experiment family out
dominant pull
posterior predictive check
```

Entscheidungslogik:

```text
theta13 > 3σ allein: PMNS θ13 transfer corrected
zwei Seed Beine > 3σ: Shared Seed Block fällt
beta > 3σ weg: CMB birefringence leg fällt
```

### P1: Inflation Branch Resolver

Der Branch Resolver muss jetzt eine echte Entscheidung erzwingen:

```text
Mode A: fixed N⋆ = 51.4
Mode B: profiled N⋆ in [50,60]
Mode C: reheating prior from microphysics
```

Output:

```text
n_s, r, A_s
joint likelihood Planck only
joint likelihood Planck + ACT + DESI
Bayes factor fixed vs profiled
CMB S4 forecast
```

Wenn `N⋆=51.4` nur Preferred Branch ist, sollte `A_s fixed` nicht als volle Prediction of Record zählen. Wenn es volle Prediction ist, dann ist es eine echte Spannung und muss so stehen bleiben.

### P1: Dark Energy w Watchdog

Baut einen eigenen Runner:

```text
experiments/dark-energy-w-watchdog/
```

Mit:

```text
ΛCDM
wCDM
CPL w0-wa
DESI DR2 BAO
CMB prior
Pantheon+, Union3, DESY5 getrennt
overlap-aware combination
```

Nicht die drei SN Kombinationen einfach multiplizieren. Genau dort entstehen gerne Scheinsignifikanzen mit Doktorhut.

Kill:

```text
w != -1 bei >=5σ in systematik-kontrollierten, overlap-aware Kombinationen
```

### P1: Axion Spine Finite T Solve

Jetzt rechnen, nicht mehr diskutieren.

Frozen Inputs:

```text
fa = Mscal / 128
theta_i = 3π/5
NDW = 1
chi(T) lattice exponent
acceptance: 0.08 <= Ωa h² <= 0.16
```

Output:

```text
Ωa h²
sensitivity to chi(T)
sensitivity to g*(T)
anharmonic factor
branch verdict
```

Kein Nachjustieren. Wenn Spine fällt, fällt der dominante Axion DM Branch. Das wäre kein Weltuntergang, nur ein Asteroid für einen Branch.

Nebenbefund (v429, DM.AXION.PENTAGON.01, [C] Brücke, kein Statuswechsel): weil `N_fam = g_car - 2`, ist `theta_i = (g_car-2)π/g_car` exakt der Innenwinkel des regulären `g_car`-Ecks — für `g_car=5` das Pentagon, also `theta_i = 3π/5 = 108°`, `cos(theta_i) = (1-√5)/4 = -1/(2φ)`. Der goldene Charakter ist eindeutig an `g_car=5` gebunden (n=3,4,6 geben rational 1/2,0,-1/2). Damit ist die sonst „unmapped" goldene/ikosaedrische E8-Struktur (v354/v313) genau die Geometrie dieses einen externen kosmologischen Inputs — `DM.AXION.SPINE.01` bleibt [C], kein Gate geschlossen.

### P1: Rare Kaon Joint Flavor Bridge

Nicht nur K⁺ Einzelwert.

```text
BR(K+)
BR(KL)
R_K = 0.35238
δ_CKM / γ
Jarlskog
Grossman Nir bound
SM short distance nuisance parameters
```

Ziel: Testet die Flavor Bridge als Geometrie, nicht einen einzelnen Glückstreffer.

### P2: GW Strain Level Echo

Stage 1 erst starten, wenn ihr echte Strain Pipeline habt.

Minimum:

```text
High ringdown SNR events
Kerr QNM subtraction
residual matched filter
lag free
phase free
q fixed at 64/729
free q control
injection recovery
stacked Bayes factor
```

Interpretation:

```text
free q gewinnt fern vom Kernel: kein TFPT Signal
q deutlich größer als 64/729 über mehrere Events: Kanal fällt
kein Echo trotz Sensitivität: null
```

### P2: Higgs Near Criticality RGE

Das ist sehr lohnend, weil es einen echten Standard Model Running Kanal trifft.

```text
λ(MPl)
βλ(MPl)
scan mt, αs, mH
pull to double critical surface
```

Nicht als Compiler Output, sondern downstream Bridge. Der Frontier Text hält genau diese Grenze: Frontier und QCD/Kosmologie Transfers dürfen nicht zu primitiven Compiler Outputs gemacht werden. 

### P2: Leptogenesis Scalaron Decuple

Frozen Route:

```text
M1 = Mscal φ0² / AΛ
mtilde1 = m3 / AΛ
AΛ = 10
δνCP = 4π/3
```

Full flavored Boltzmann, nicht nur eine Estimate Formel.

Kill:

```text
ηB = 6.1e−10 um Faktor >3 verfehlt
```

### P2: QGEO DtN Mark Locality

Nicht in die empirische Scorecard. Theory Contract.

Ziel:

```text
Λ = |Dθ| + Mf
f(θ) = Σ_j g(θ − jπ/2)
=> Fourier support only 0 mod 4
=> [ρ, Λ] = 0
=> ω ∘ ρ = ω
```

Negative Controls:

```text
Z3 marks
4 generic marks
off character matrix elements
```

Das ist weniger glamourös als EHT, aber vermutlich fundamentaler. Mathematische Arbeit hat leider selten Nebelmaschine.

## 5. Meine Gesamtbewertung

### Wissenschaftliche Disziplin: 8.5 von 10

Die Typisierung ist stark. Firewall, Stage, Status, Evidence Class, Independence Groups, Kill Conditions: Das ist genau die richtige Struktur. Besonders gut ist, dass interne Checks und korrelierte Seed Beine nicht als unabhängige externe Treffer zählen. 

### Empirische Stützung: 5.5 von 10

Es gibt viele schöne Konsistenzen, aber noch keine unterscheidende Signatur. Die besten positiven Punkte sind `θ12`, K⁺, CMB β und Λ Hierarchie. Aber jeder hat eine Einschränkung: θ12 braucht weitere JUNO Präzision, K⁺ ist downstream, β hat Systematiken, Λ ist Metrologie gefährdet.

### Falsifizierbarkeit: 8 von 10

Sehr gut. `θ13`, `A_s`, `r`, `w`, EHT, K⁺, g minus 2 Baseline und Axion Spine können echte Branches beschädigen. Das ist ein gutes Zeichen. Eine Theorie ohne Sollbruchstellen ist meistens nur ein sehr höflicher Mythos.

### Aktuelles Risiko: mittel bis hoch

Die vier Risikopunkte sind nicht dekorativ:

`A_s fixed N⋆=51.4` ist stark gespannt.
`θ13` ist der Seed Stress.
`g−2` kippt mit Lattice HVP.
`Axion Hilltop` ist überproduzierend.

Keiner davon tötet den Compiler Kern. Aber alle vier zwingen euch, die Bridge Typisierung ernst zu nehmen.

## 6. Konkrete nächste Agent Anweisung

```text
Branch: empirical-v4-decision-tests

P0:
1. Merge duplicate axion spine rows.
2. Update shared seed chi2/dof to one canonical value.
3. Replace shared seed kill condition with the 3σ leg rule and two-leg kill rule.
4. Change EHT independence_group from phi0_seed to c3_topform_horizon.
5. Replace Lambda pull_sigma with log_order_deviation.
6. Update signature catalog EHT wording: real ingest done, GRMHD residual open.
7. Add composite-row fields: chi2_dof, max_leg_pull_sigma, dominant_leg.

P1:
8. Run EHT imaging residual pipeline with GRMHD subtraction.
9. Build seed-consistency v2 with reactor-only theta13 and covariance.
10. Build inflation branch resolver with fixed vs profiled Nstar Bayes comparison.
11. Build dark-energy-w-watchdog with overlap-aware DESI DR2 combinations.
12. Run axion spine finite-T solve without tuning.
13. Build rare-kaon joint bridge, including R_K and CKM nuisance parameters.

P2:
14. Start GW strain-level echo only after injection and free-q controls exist.
15. Run Higgs criticality RGE nuisance scan.
16. Run scalaron-decuple leptogenesis Boltzmann solve.
17. Move QGEO DtN mark locality into theory-contracts, not evidence_scorecard.
```

## Bottom line

Der Stand ist jetzt seriös genug, um externe Kritik auszuhalten, aber noch nicht stark genug, um “empirisch bestätigt” auch nur zu flüstern. Die richtige Aussage ist:

> **TFPT hat einen bemerkenswert kohärenten, typisierten Konsistenzkörper, mehrere echte Stresspunkte und inzwischen gute Kill Tests. Die nächsten Entscheidungen kommen nicht aus mehr Suchräumen, sondern aus härteren Läufen: EHT Residual, Seed θ13, Inflation N⋆, DESI w, Axion Spine und Ftransfer Solver.**

Das ist eine gute Lage. Nicht bequem, aber gut. Bequeme Theorien schlafen ein. This one is at least caffeinated.

[1]: https://arxiv.org/abs/2509.13654?utm_source=chatgpt.com "Cosmic Birefringence from the Atacama Cosmology Telescope Data Release 6"
[2]: https://indico.global/event/484/contributions/143473/attachments/67640/131077/JUNO_dbick_LLWI26.pdf?utm_source=chatgpt.com "JUNO – Status and First Results"
[3]: https://ligo.org/detections/o4b-catalog/?utm_source=chatgpt.com "O4B CATALOG – LSC - LIGO Scientific Collaboration"
[4]: https://arxiv.org/abs/2604.12649?utm_source=chatgpt.com "New measurement of $K^+\toπ^+ν\barν$ branching ratio at ..."
[5]: https://link.aps.org/doi/10.1103/PhysRevLett.134.081802?utm_source=chatgpt.com "Search for the Decay at the J-PARC KOTO Experiment"
[6]: https://arxiv.org/abs/2506.03069?utm_source=chatgpt.com "Measurement of the Positive Muon Anomalous Magnetic Moment to 127 ppb"
[7]: https://arxiv.org/html/2504.15336v1?utm_source=chatgpt.com "On DESI's DR2 exclusion of ΛCDM"

## Update 2026-06-17: drei neue Black-Hole-Cosmology-Signaturen aus problem_b.txt

Aus dem Black-Hole-Cosmology-Teil von problem_b.txt drei datenkonfrontierbare Signaturen als
self-contained experiments/-Runner gebaut (der Rest von problem_b ist verification/-Mathematik,
kein Datentest). Scorecard jetzt 50 Zeilen (24 consistent, 7 null, 14 data_limited, 4 tension,
1 parked). Alle drei bewusst konservativ getypt, keine als Entdeckung:

1. ccbh-dark-energy (downstream bridge, data_limited): de-Sitter-Seam-Innere w_in=-1 =>
   Croker-Weiner-Kopplung k=-3 w_in=3 exakt => Populations-EoS w_eff=-1. Gegen Farrah+2023
   k=3.11+/-0.79 => -0.14σ; Dichte Omega_de~0.68 vs Planck 0.6889 => -0.09σ. ABER CCBH-als-DE
   ist umstritten (Lacy/Amendola/Andrae&El-Badry/Mistele) => Bridge, kein Treffer. Ist der
   Mechanismus hinter w=-1, das dark-energy-w-watchdog gegen DESI prüft => alternative_group
   "w_de_eos" (CCBH + DESI-w zählen NICHT als zwei unabhängige Treffer). Kill: robustes k!=3 bei
   >=3σ.
2. gravastar-compactness (search target, data_limited): Nariai Q_geom=3/8 == Jampolski-Rezzolla
   2026 (arXiv:2509.15302) Max-Kompaktheit C=3/8, geteiltes de-Sitter-Ende 1/2 -- exakter
   Rational-Match, aber KEINE bewiesene C<->Q_geom-Abbildung => [C], struktureller Echo. Fenster
   1/3<3/8<4/9<1/2 => horizontloser lichtfangender ECO. Echo-Template: Tortoise-Round-Trip-Delay
   Dt=2.288 M -> ~0.70 ms (62 Msun), Amplitude <= (2/3)^6. Liefert gw-ringdown-echo die fehlende
   Zeitskala. EHT-Schatten b_c=3 sqrt3 M Kerr-entartet -> Echos, nicht Schatten, sind der
   Diskriminator.
3. cosmic-handedness (Frontier-Watchdog, data_limited): TFPT erlaubt nur winzigen
   mu4/PSL(2,C)-Orientierungsrest => approximate Parität, KEINE ~20%-Asymmetrie. Shamir JADES
   158:105 = ~3.3σ Monopol, Galaxy-Zoo isotrop. Monopol (echte Parität) vs MW-Aberrations-Dipol
   (Systematik) ungelöst -- braucht himmelsaufgelöste Zählungen. Nicht hochgestuft. Flag: globaler
   paritäts-ungerader Spin-Monopol, der MW-Aberration + Selektion über Surveys übersteht.

Befund: kein neuer unterscheidender Treffer. CCBH ist die stärkste neue Brücke (verbindet
Lambda-Engine, w=-1 und reale SMBH-Wachstumsdaten), bleibt aber wegen der Literatur-Kontroverse
data_limited; die Entscheidung fällt über DESI w(z). Gravastar schärft die GW-Echo-Suche um eine
konkrete (Delay, Amplitude)-Vorlage. Händigkeit bleibt Frontier (Pixelhaufen-Warnung operativ).

## Update 2026-06-17 (II): drei Durchbruch-Kandidaten gebaut (a/b/c); (d) war schon da

Aus der Durchbruch-Liste a-d umgesetzt. (d) RR-Index-Gate ENTFÄLLT: existiert bereits als
verification/v228_rr_index_gate.py (QGEO.RR.01) im parallelen v219-v237-Strang -- nicht dupliziert.
Die anderen drei sind experiment-seitig (kollidieren nicht mit dem verification-Strang). Scorecard
51 Zeilen (26 consistent, 7 null, 13 data_limited, 4 tension, 1 parked).

(c) eta_B FULL ODE -- der stärkste neue Befund. fboltzmann_solve.py integriert die BDP-Boltzmann-
Gleichungen (statt der analytischen kappa_f-Fit des Strips): integrierte Effizienz kappa_f=0.092
(vs Fit 0.074, validiert den Strip), und am EINGEFRORENEN M1=M_scal phi0^2/A_Lambda=8.6e9 GeV mit
delta_CP=4pi/3 ergibt sich eta_B=6.5e-10 vs beobachtet 6.1e-10 -- FAKTOR 1.07, ohne freien M_R-Dial.
Der frühere "Solve pending"/data_limited ist erledigt -> consistent (downstream bridge, [C]: Washout
m~1=m3/A_Lambda verankert, M1 via Scalaron-Route). Das ist eine echte Schärfung: der Strip liess M_R
frei und bound M1>=8.8e9 von oben; jetzt landet die FROZEN-Skala bei 1.07x beobachtet.

(b) Petz-Recovery + rank-one Baby-Universe -- recovery-channel/petz_recovery.py (CLI: tfpt-recovery
petz), Companion zu v221_seam_qecc (das den KL-Kontraktionsbound liefert, Petz-Identifikation [C]
aufgeschoben). Numerisch: ||T^n - P_inf|| = (2/3)^{6n} EXAKT (rank-one Kollaps am Kernel-Tempo = der
1-dim Baby-Universe-Hilbertraum, Engelhardt 2025/JHEP 12(2025)159); expliziter Petz-Operator R_P ist
CPTP + recovers reference, nur die lambda=1-Mode fuer alle Zustaende (KL); Negativkontrollen (freies
Ratio, entartetes Spektrum) greifen. internal_consistency, kein neues Datum -- die staerkste
Theorie-Bruecke zu Mainstream-QG (problem_b sec.H), jetzt realisiert statt nur behauptet.

(a) GW-Echo Stage-1 -- gw-ringdown-echo/echo_search.py (CLI: tfpt-gw search). Echte Matched-Filter-
Maschinerie: Kerr-Ringdown-Subtraktion -> MF auf Residuen (Lag/Phase frei, Ratio (2/3)^6 fix) ->
Detection-SNR + freier-q-Kontrolltemplate. Auf synthetischem Strain 3/3 Injektionen korrekt
klassifiziert (Kernel-Echo->DETECTION q_hat~(2/3)^6; kein Echo->NULL; Ratio 0.5->NON_KERNEL_ECHO).
Template-Lag aus gravastar-compactness (~0.7ms, C=3/8); Lag gescannt, Ratio der Diskriminator. Bleibt
data_limited bis echtes GWOSC-Strain (gwpy) -- kein Echo-Claim, validierte Pipeline (EHT-Muster).

Befund: kein neuer unterscheidender empirischer Treffer (wie erwartet). Aber eta_B@frozen-M1 (1.07x)
ist ein bemerkenswerter Near-Hit, die Petz/Baby-Universe-Bruecke ist die sauberste QG-Anbindung, und
die GW-Echo-Pipeline ist jetzt bereit fuer echtes Strain. Website EXPERIMENTS_AUDIT auf 51/26/13
gesynct. verification/ (v219-v237) bewusst NICHT angefasst -- das ist der parallele Strang.
