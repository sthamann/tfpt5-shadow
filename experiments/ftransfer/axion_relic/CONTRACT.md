# F_relic — axion misalignment → Ω_a

**source inputs** — the determinant-line axion `f_a = M_scal/(2 dim S⁺ |μ₄|) = M_scal/128 ≈ 2.39×10¹¹ GeV`,
`m_a ≈ 23.8 µeV`, the seam misalignment angle `θ_i = π(1−φ_seam(α⋆)) ≈ 170.4°`, `N_DW = 1`
(suite `v25`, `v185`). No WIMP: the `128 = (16,4)+(16,4)` spinor leaves no free E₈ singlet.

**scheme** — finite-temperature misalignment with the QCD topological susceptibility `χ(T)`; large-angle
(anharmonic) enhancement near the hilltop; string/domain-wall contribution for `N_DW = 1`.

**transfer** — `F_relic : (f_a, m_a, θ_i, N_DW) ↦ Ω_a h²`. Solve `θ̈ + 3H(T)θ̇ + m_a²(T) sinθ = 0` from
above the QCD crossover; add `Ω_a^str(f_a, N_DW)`.

**observables** — `Ω_a h²` (target `Ω_DM h² = 0.12`).

**kill condition** — if a proper finite-T relic solve cannot reach `Ω_DM` for plausible `θ_i` near the
hilltop (where the relic is exponentially sensitive), the determinant-line axion *branch* falls — not the
theory.

**status** — `[C]` scenario, hilltop-sensitive. The harmonic estimate under-produces at this `f_a` (~21 %),
so the `θ_i ≈ 170°` anharmonic enhancement is *required*; `f_a = M_scal/128` stays a conjecture.
**Do not** paste a "nice" `Ω_a` before the integral runs (`v185` discipline). The rejected `θ_i = π−3φ₀`
(`π≈3` coincidence) must **not** be reintroduced.
