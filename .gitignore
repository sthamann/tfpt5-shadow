.DS_Store

# Python
__pycache__/
*.py[cod]
*$py.class
*.so
.Python
*.egg-info/
.eggs/
dist/
build/
.pytest_cache/
.mypy_cache/
.ruff_cache/

# Auto-generated build revision stamp (regenerated by build.sh each build)
tex-artefacts/version-auto.tex
tex-artefacts/release-version.txt

# LaTeX build artefacts (removed by build.sh after each successful compile)
*.aux
*.log
*.out
*.toc

# Local drafts / correspondence
list.txt
experiments/older_tests/befund*
alessandro*.html

# Local problem / working notes (not shipped)
problem_*

# External tools cloned locally (reproduced on demand by verification/pyrate/reproduce.sh)
experiments/pyrate/
verification/pyrate/*.generated.txt

# Local secrets (Zenodo token, etc.)
.env

# Local exploratory 3D visualization sandbox (not shipped)
/simulation/

# Local scratch / explanations (not part of the published record)
temp/
_documentation/

# Local animation render pipeline (not shipped)
_render/
tfpt_animation*

# Interactive "TFPT machine" game (local-only, standalone; not part of the record)
_game/

# LinkedIn outreach drafts (article + generated banner/figures); local-only, not shipped
/linkedin/

# Three.js / Rapier simulation (local-only; not part of the published record)
simulation/

# FRB experiment datasets (local copies; see data/README.md)
# keep the tiny derived/provenance files so FRB.01 stays reproducible
experiments/frb-tfpt-signatures/data/*
!experiments/frb-tfpt-signatures/data/README.md
!experiments/frb-tfpt-signatures/data/frb01_subband_toas.csv
experiments/frb-tfpt-signatures/new-data/

# Outreach drafts (mini-paper + email + Wolfram-Language verification for S. Wolfram);
# local-only, not part of the published record. NB: anchored "/wolfram/" so it does
# NOT match the tracked verification/wolfram mirror.
/wolfram/